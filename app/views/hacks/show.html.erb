<%= image_tag @hack.image.url(:medium) %>

<p>
  <strong>Description:</strong>
  <%= @hack.description %>
</p>

<% if @hack.user == current_user %>
	<%= link_to 'Edit', edit_hack_path(@hack) %> 
<% end %>
<%= link_to 'Back', hacks_path %>

